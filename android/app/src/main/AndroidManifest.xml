<?xml version="1.0" encoding="utf-8"?>
<!--
  DSI Vault — manifeste fusionné avec le projet Android réel (namespace déclaré
  dans app/build.gradle : com.dsivault.app).

  Étape actuelle (infrastructure + build debug) :
  - Le service react-native-track-player n'est PAS déclaré (ajouté à l'étape Audio).
  - android:supportsPictureInPicture n'est PAS déclaré (ajouté à l'étape PiP).
  - Aucun accès SAF, aucun lecteur natif branché à ce stade.
  Les permissions ci-dessous restent des déclarations statiques déjà présentes
  dans le manifeste d'origine ; elles ne déclenchent aucune fonctionnalité tant
  qu'aucun code ne les utilise.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Android 13+ (API 33) : permissions granulaires -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Android < 13 : permission historique (dépréciée progressivement) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        android:allowBackup="false"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|uiMode"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Réception de fichiers depuis d'autres applications (Partie 2 §26) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="audio/*" />
                <data android:mimeType="video/*" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!--
          Service react-native-track-player — volontairement NON déclaré à cette
          étape (décision validée). À ajouter lors de l'étape Audio :

          <service
              android:name="com.doublesymmetry.trackplayer.service.MusicService"
              android:foregroundServiceType="mediaPlayback"
              android:exported="false" />
        -->

    </application>
</manifest>